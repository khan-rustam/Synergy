import React from 'react';

// This is a placeholder component replacing the deleted EventsSection
// It's an empty component that doesn't render anything
// This helps resolve Vite build errors related to CSS processing

const EventsSection: React.FC = () => {
  return null;
};

export default EventsSection; 